#include "boyermoore.h"

BMHorspool::BMHorspool(const string &naald) : naald(naald)
{
    // VUL AAN
}

std::queue<int> BMHorspool::zoek(const string &hooiberg)
{
    laatsteAantalKaraktervergelijkingen = 0;
    std::queue<int> locaties;

    // VUL AAN

    return locaties;
}
